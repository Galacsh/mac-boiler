#!/usr/bin/env bash

cd -- "$(dirname -- "${BASH_SOURCE[0]}")" || exit

# =========================================

./rust.sh || exit 0

# =========================================

./go.sh || exit 0

# =========================================

./nvm.sh || exit 0

# =========================================

./sdkman.sh || exit 0
